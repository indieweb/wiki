https://indieweb.org/telnet_protocol

{{stub}}

'''<dfn>telnet protocol</dfn>''' is the protocol used to allow clients to connect to a remote server over a TCP/IP network. While it's called a protocol, in reality it's a client program that is used to make socket connections and present the user with a prompt.
